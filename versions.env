# Single source of truth for pinned non-Go tool versions (shell-sourceable).
# Loaded by the Taskfile (dotenv), the Dockerfile (COPY + source), and CI.
# templ is NOT here — its version lives in go.mod and is derived via `go list -m`.
TAILWIND_VERSION=4.3.1
