{
  "rules": [
    {
      "description": "(b) default: respect origin Cache-Control (the Datastar pages are origin no-cache, so the edge revalidates the entry document)",
      "expression": "true",
      "action": "set_cache_settings",
      "action_parameters": {
        "cache": true,
        "edge_ttl": { "mode": "respect_origin" },
        "browser_ttl": { "mode": "respect_origin" }
      }
    },
    {
      "description": "(a) /events suffix bypass cache so the SSE stream un-buffers (defense in depth over the origin no-cache + X-Accel-Buffering headers)",
      "expression": "ends_with(http.request.uri.path, \"/events\")",
      "action": "set_cache_settings",
      "action_parameters": { "cache": false }
    }
  ]
}
