{
  "rules": [
    {
      "description": "(c) default: respect origin Cache-Control (year-long immutable /assets/*, revalidate index.html via no-cache)",
      "expression": "true",
      "action": "set_cache_settings",
      "action_parameters": {
        "cache": true,
        "edge_ttl": { "mode": "respect_origin" },
        "browser_ttl": { "mode": "respect_origin" }
      }
    },
    {
      "description": "(b) /api/* bypass cache (defense in depth over F4; mutations + /api/cards must never cache)",
      "expression": "starts_with(http.request.uri.path, \"/api/\")",
      "action": "set_cache_settings",
      "action_parameters": { "cache": false }
    },
    {
      "description": "(a) /events suffix bypass cache so SSE streams un-buffered (covers /api/events AND /ds/events)",
      "expression": "ends_with(http.request.uri.path, \"/events\")",
      "action": "set_cache_settings",
      "action_parameters": { "cache": false }
    }
  ]
}
