# --- Secrets / env (never commit) ---
.env
.env.*
!.env.example

# --- Go ---
# air hot-reload build dir
/tmp/
*.test
*.out
coverage.*

# --- Logs ---
*.log

# --- templ generated ---
# Regenerated by `templ generate` in dev and in the Docker build (see PRD §5).
*_templ.go

# --- Local Postgres dumps / scratch ---
*.dump
*.sql.bak

# --- Editor / OS cruft ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~

# compiled binary (go build ./cmd/unbusy default output)
/unbusy
